"use strict"; // treat all JS code as newer version

// alert(3 + 3) // we are usig  node js, not browser

console.log(3 + 3); // code readability should be high

console.log("ALMAS ");

/* Thare are somne diffrenets type of data types in java scripts

numner = 2 to 255

bigint (use in large number of web sites)

string = " " 

boolean = true / faise ,yes/ no

null = standalm9ne value (object

undifined =

symbol = unoquness

*/

// objest
console.log(typeof null);
